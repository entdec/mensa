th
  .container
    .title = column.human_name
    .actions
      - if column.sortable?
        .action
          = link_to table.path(order: column.sort_field, turbo_frame_id: table.table_id), "data-turbo-frame": "_self"
            i class=Mensa.config.icons["order_indicator#{column.sort_direction.to_s.present? ? "_#{column.sort_direction.to_s}" : ""}".to_sym]
